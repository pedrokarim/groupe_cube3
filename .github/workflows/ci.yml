# ============================================================
# 🚀 CI SIMPLE - GitHub Actions
# ============================================================
# Push ce fichier sur GitHub et regarde l'onglet "Actions" !
# ============================================================

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:  # Bouton manuel

jobs:
  test:
    name: ✅ Test
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout du code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: npm install

      - name: 🔷 TypeScript check
        run: npm run typecheck

      - name: 🔍 ESLint
        run: npm run lint

      - name: 🧪 Tests
        run: npm test

      - name: 🏗️ Build
        run: npm run build

      - name: 🎉 Success
        run: echo "✅ Tout est OK !"
