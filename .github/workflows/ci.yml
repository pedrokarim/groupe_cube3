name: CI

on:
  push:
    branches: [main]        # À chaque push sur main
  pull_request:
    branches: [main]        # À chaque PR vers main
  workflow_dispatch:         # Bouton "Run workflow" manuel

jobs:
  check:
    name: ✅ Vérification du code
    runs-on: ubuntu-latest   # Machine virtuelle Linux gratuite

    steps:
      # Étape 1 : Télécharger le code du repo
      - name: 📥 Checkout
        uses: actions/checkout@v4

      # Étape 2 : Installer Node.js
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm         # Cache les node_modules

      # Étape 3 : Installer les dépendances
      - name: 📦 Install
        run: npm ci          # Comme npm install mais plus rapide en CI

      # Étape 4 : Vérifier TypeScript
      - name: 🔷 TypeScript
        run: npm run typecheck
        continue-on-error: true  # Continue même si ça échoue

      # Étape 5 : Linter (ESLint)
      - name: 🔍 Lint
        run: npm run lint

      # Étape 6 : Tests
      - name: 🧪 Tests
        run: npm test
        continue-on-error: true  # Continue même si pas de tests

  build:
    name: 🏗️ Build
    runs-on: ubuntu-latest
    needs: check             # Attend que "check" soit fini

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: 📦 Install
        run: npm ci

      - name: 🏗️ Build
        run: npm run build
